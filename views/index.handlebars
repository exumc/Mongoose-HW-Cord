{{#each dbArticle}}
<div class="row">
  <div class="s12">
    <div class="card mt-4">
      <div class="card-body">
        <h5 class="card-title">Article</h5>
        <p>
          <strong>Title</strong>: {{title}}</p>
        <p>
        <p>
          <strong>Link</strong>: {{link}}</p>
      </div>
    </div>
  </div>
</div>
{{/each}}